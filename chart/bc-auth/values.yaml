# Default values for micro-auth.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 2
image:
  repository: registry.ng.bluemix.net/kyanite_int/micro-auth
  tag: 16
  pullPolicy: Always
service:
  name: auth-service
  type: ClusterIP
  externalPort: 8080
  internalPort: 8080
hs256key:
  secretName: hs256-key
resources:
  limits:
    cpu: 1000m
    memory: 512Mi
  requests:
    cpu: 1000m
    memory: 512Mi

